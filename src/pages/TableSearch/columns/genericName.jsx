import * as React from "react"

export default function genericNameColumn() {
    return {
        key: "generic_name",
        label: "Generic Name",
        columnAlign: "left",
        headAlign: "left",
        maxWidth: 200,
    }
}